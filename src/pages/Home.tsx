// Make sure the path is correct
import HeroSection from "../components/Hero";


const Home = () => {
  return (
    <div>
      <HeroSection />
      {/* Add future sections like Services, Departments, About, Contact below */}
    </div>
  );
};

export default Home;
